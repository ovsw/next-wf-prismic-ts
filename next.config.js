/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "uploads-ssl.webflow.com",
      "images.unsplash.com",
      "images.prismic.io",
    ],
  },
};

module.exports = nextConfig;
